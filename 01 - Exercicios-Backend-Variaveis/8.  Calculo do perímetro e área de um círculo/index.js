const r = 1
const calculoComprimento = 2 * Math.PI * r
const calculoArea = Math.PI * r ** 2

console.log(`O comprimento da àrea possui o valor aproximado de ${calculoComprimento} e a, sua àrea possui o valor aproximado de ${calculoArea}.`)

//resolucao do professor
const raio = 1;

const comprimento = 2 * Math.PI * raio;

const area = Math.PI * raio ** 2;

console.log(`A área do circulo é ${area} e o comprimento é ${comprimento}`)
