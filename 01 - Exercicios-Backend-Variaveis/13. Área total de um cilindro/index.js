{
    const r = 3
    const h = 1
    const at = (2 * r * (r + h)).toFixed(0)

    console.log(`A área total de um clilindro a partir do raio da sua base e da sua altura é de ${at}`)
}
//resolucao do professor 
{
    const r = 3;
    const h = 1;

    const a = 2 * Math.PI * r * (r + h);

    console.log(a);
}
