const a = 1
const b = 5
const c = 3

const delta = (b ** - 4 * a * c).toFixed(0)

console.log(`O valor de delta é ${delta}.`);

//resolucao do professor
const a2 = 1, b2 = -5, c2 = 3;

const delta2 = b2 ** 2 - 4 * a2 * c2;

console.log(`O valor de delta é ${delta2}`)